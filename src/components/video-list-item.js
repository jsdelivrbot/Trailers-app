import React from 'react'

/*const VideoListItem = (props) => {
  return <li>Un film recomande : {props.movie}</li>
}*/

const VideoListItem = ({movie}) => {
  return <li>A good movie: {movie}</li>
}

export default VideoListItem;
